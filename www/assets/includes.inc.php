<?php
/**
 * CWP Media tool
 */
/*
 * CWP Media tool.
 */

/*
 function autoloadHTML($className)
{
    $filename = __INC_CLASS_DIR__.'/HTML/'.$className.'.class.php';
    if (is_readable($filename)) {
        require_once $filename;
    }
}

function autoloadMedia($className)
{
    $filename = __INC_CLASS_DIR__.'/Media/'.$className.'.class.php';
    if (is_readable($filename)) {
        require $filename;
    }
}
function autoloadDb($className)
{
    $filename = __INC_CLASS_DIR__.'/Db/'.$className.'.class.php';
    if (is_readable($filename)) {
        require $filename;
    }
}
function autoloadExel($className)
{
    $filename = __INC_CLASS_DIR__.'/Spreadsheet/'.$className.'.class.php';
    if (is_readable($filename)) {
        require $filename;
    }
}

function autoloadClass($className)
{
    $filename = __INC_CLASS_DIR__.'/'.$className.'.class.php';
    if (is_readable($filename)) {
        require $filename;
    }
}

spl_autoload_register('autoloadHTML');
spl_autoload_register('autoloadMedia');
spl_autoload_register('autoloadDb');

spl_autoload_register('autoloadExel');
spl_autoload_register('autoloadClass');

*/
